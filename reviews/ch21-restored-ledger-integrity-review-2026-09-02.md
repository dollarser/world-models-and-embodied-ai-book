# 第21章恢复 ledger 完整性审查（2026-09-02）

## 审查结论

- **内容准确性**：通过。正文把缓存命中前的 ledger 结构校验与 durable recovery、存储完整性和物理 exactly-once 分开。
- **代码一致性**：通过。`EXP-21-01` v11 在状态转移前拒绝布尔 command ID、非有限 action、负 applied step、畸形 digest，以及 digest 与缓存字段不一致。
- **证据强度**：通过。五种损坏状态均为作者手工构造的单进程内存 fixture；没有读取 WAL、数据库或崩溃快照。
- **教学质量**：通过。`TAB-21-10` 保留五类独立失败原因，读者可区分结构 fail-closed 与经过验证的恢复协议。

## 验收边界

该改动只能证明纯函数拒绝五种指定的含糊内存状态。它不证明磁盘原子性、校验和覆盖、事务顺序、并发线性化、崩溃恢复、可信执行器回执、认证、完整性保护或物理副作用恰好一次。
