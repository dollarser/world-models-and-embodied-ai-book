# 第17章等准确率错误位置审查（2026-09-02）

## 审查结论

- **科学问题**：通过。均匀 transition accuracy 不包含错误 query 的策略访问频率、候选选择影响或真实后果。
- **配对反例**：通过。两个单故障模型在同一9格 panel 上均为8/9正确；仅移动唯一错误位置。
- **决策后果**：通过。`(0,shortcut)` 被当前候选访问一次，模型选择碰撞捷径且 regret 为1.85；`(3,wait)` 当前零访问，模型仍选 safe route 且 regret 为0。
- **证据边界**：通过。“零访问”只针对固定初态与三个候选策略，不表示该 query 对其他策略、planner 或部署分布无风险。
- **教学与驾驶迁移**：通过。新增 `TAB-17-05`、`CLAIM-17-12`、练习6与 `SELF-CHECK-17-06`，驾驶正文要求分别报告 benchmark、policy occupancy、planner query 与严重度分层。

## 验证范围

- 第17章单元测试：23项；
- smoke 与中央结果要求精确一致；
- 全书目标计数：212条声明、120条结果、408个章节测试、142道练习；
- 完整门禁在提交前运行；没有 learned world model、真实策略、仿真、GPU、机器人或车辆。
