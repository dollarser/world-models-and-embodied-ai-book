# 第11章 endpoint cancellation 与逐轨迹分母审查（2026-09-02）

## 审查结论

- **内容准确性**：通过。正文把平均终点误差、逐序列终点和逐 horizon 轨迹误差分开，不再仅用“可能抵消”的文字提醒替代实例。
- **实验合同**：通过。`EXP-11-01` v3 保留3条预设未见序列和9个转移，额外登记终点误差为0但中间误差非零的序列数及最大隐藏误差。
- **代码一致性**：通过。left-right-swapped 在 `left→forward→right` 上的 oracle/swapped y 轨迹为 `3→2→2→3` 与 `3→4→4→3`；终点误差0，中间最大误差2。
- **教学质量**：通过。新增 `TAB-11-05`、`CLAIM-11-11`、练习6与 `SELF-CHECK-11-06`，要求预登记抵消序列并保留逐 horizon、失败与终止字段。
- **证据边界**：通过。1条触发只是在3条手写序列中的精确计数，不是现实错误率；网格距离不是视频、车辆或机器人物理单位。

## 指标为何不能互相替代

跨序列平均终点误差为2，并不能告诉读者其中恰有一条序列的终点误差为0。反过来，单条序列终点正确也不能恢复两步中间偏差。正式结果因此同时保留：序列数、转移数、全轨迹RMSE、平均终点误差、endpoint-cancellation count 与 maximum hidden intermediate error。

## 验证范围

- 第11章单元测试：15项；
- smoke 与中央结果：要求精确一致；
- 全书目标计数：206条声明、114条结果、383个章节测试、136道练习；
- 完整门禁在提交前运行；没有训练视频模型、下载 checkpoint、运行仿真、GPU、机器人或车辆。
