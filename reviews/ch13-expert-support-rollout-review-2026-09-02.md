# 第13章 expert support 与扰动 rollout 审查（2026-09-02）

## 审查结论

- **内容准确性**：通过。正文不再只用无反馈积分器解释 covariate shift，而是显式拆开 expert-support loss 与支持集外 rollout。
- **实验合同**：通过。`EXP-13-01` v4 固定唯一专家点、同一标量转移、同一 reset disturbance、六步 horizon 与正负反馈增益；两个策略只改变 off-support action direction。
- **代码一致性**：通过。两个手写策略在 `(x=0,a*=0)` 上 action MSE 都为0；从 `x_0=0.25` 出发，六步最终绝对状态精确为0.00390625与2.84765625。
- **教学质量**：通过。新增 `TAB-13-04`、`CLAIM-13-08`、练习6与 `SELF-CHECK-13-06`，要求读者同时报告支持范围、扰动分桶和 rollout 后果。
- **证据边界**：通过。策略不是从单点数据训练所得；结果不估计 BC/ACT、DAgger 收益、真实扰动概率、稳定域、机器人或车辆安全。

## 关键负对照

| 计分/执行域 | negative feedback | positive feedback |
| --- | ---: | ---: |
| expert-support action MSE | 0 | 0 |
| 无扰动、从 `x=0` rollout | 轨迹恒为0 | 轨迹恒为0 |
| `x_0=0.25` 六步最终绝对状态 | 0.00390625 | 2.84765625 |

只有在预先声明的支持集外 reset 条件下，两个同分策略才被区分；这说明离线零误差是局部证据，不是闭环泛化证据。

## 验证范围

- 第13章单元测试：18项；
- smoke 与中央结果：要求精确一致；
- 全书目标计数：208条声明、116条结果、392个章节测试、138道练习；
- 完整门禁在提交前运行；没有 LeRobot、数据下载、训练、仿真、GPU、机器人或车辆。
