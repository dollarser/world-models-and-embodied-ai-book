{
  "schema_version": 2,
  "id": "EXP-22-01",
  "chapter": 22,
  "status": "smoke",
  "claim_ids": ["CLAIM-22-02", "CLAIM-22-07", "CLAIM-22-08", "CLAIM-22-10"],
  "claim_scope": [
    "a complete fixed driving project package passes the fixture audit while an intentionally invalid package triggers twenty-three named contract issues",
    "a five-stage cross-chapter evidence trace is accepted only when every stage has a chapter-matching artifact identifier, revision, decision, and frozen dependency set",
    "five in-memory artifact payloads are accepted only when URI, SHA-256 digest, producer stage, and exact claim binding agree",
    "train selection and evaluation partitions must each declare group source-asset exact-content and authored near-duplicate-cluster identities and reject cross-partition overlap"
  ],
  "code": {
    "repository": "cv2embodied",
    "commit": "UNCOMMITTED",
    "config": "standard-library v4 project-package, four-dimensional split-identity, artifact-binding, and five-stage traceability audit fixture"
  },
  "data": {
    "name": "two hand-authored in-memory project package fixtures",
    "version": "v4",
    "license": "MIT",
    "classification": "fixture",
    "fixture": true,
    "download_bytes": 0
  },
  "resources": {
    "tier": "S",
    "device": "CPU",
    "duration_seconds": 0,
    "peak_vram_bytes": null,
    "disk_bytes": 0,
    "gpu_verified": false
  },
  "commands": {
    "smoke": "make ch22-smoke",
    "evaluate": "python3 labs/track-d-capstone/ch22-project-audit/scripts/smoke.py",
    "report": "python3 labs/track-d-capstone/ch22-project-audit/scripts/smoke.py"
  },
  "metrics": [
    {"name": "valid_package_issue_count", "value": 0, "unit": "issues", "split": "valid"},
    {"name": "invalid_package_issue_count", "value": 23, "unit": "issues", "split": "invalid"},
    {"name": "required_artifact_count", "value": 5, "unit": "artifacts", "split": "contract"},
    {"name": "required_driving_metric_count", "value": 3, "unit": "metrics", "split": "contract"},
    {"name": "required_trace_stage_count", "value": 5, "unit": "stages", "split": "contract"},
    {"name": "split_identity_dimension_count", "value": 4, "unit": "dimensions", "split": "contract"},
    {"name": "verified_artifact_binding_count", "value": 5, "unit": "artifacts", "split": "valid"},
    {"name": "verified_failure_injection_count", "value": 2, "unit": "injections", "split": "valid"}
  ],
  "artifacts": ["results/ch22/EXP-22-01-smoke.json"],
  "limitations": [
    "in-memory dictionaries and fixed text payloads rather than a real project directory or reproduction run",
    "digest and dependency checks detect fixture drift but do not establish scientific correctness, semantic validity, privacy, licensing, or safety",
    "declared split identities do not discover unregistered semantic near duplicates or prove statistical independence",
    "trace revisions are authored metadata and this in-memory audit does not resolve upstream experiment cards to confirm them",
    "the resource policy encodes this book's limits rather than universal hardware requirements",
    "no model, dataset, training, simulator, GPU, robot, vehicle, or deployment"
  ]
}
